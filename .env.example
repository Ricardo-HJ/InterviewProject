# Credentials for the three mock Provider APIs.
# Copy to `.env` (the dev runner loads it automatically). The mock APIs read these to
# decide which credentials they accept, and YOUR proxy will need them to authenticate.

ATLAS_API_KEY=atlas-secret-key
BEACON_API_KEY=beacon-key-123
COBALT_TOKEN=cobalt-bearer-token
